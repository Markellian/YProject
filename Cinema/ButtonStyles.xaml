<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Определение цветов -->
    <SolidColorBrush x:Key="PurpleColor" Color="#8A2BE2"/>
    <SolidColorBrush x:Key="PinkHoverColor" Color="#FFC1E3"/>
    <SolidColorBrush x:Key="PinkPressedColor" Color="#FFA8D5"/>
    <SolidColorBrush x:Key="DisabledBackgroundColor" Color="#F0F0F0"/>
    <SolidColorBrush x:Key="DisabledForegroundColor" Color="#B39DDB"/>

    <!-- Стиль для кнопки с белым фоном и фиолетовым текстом -->
    <Style x:Key="PurpleWhiteButton" TargetType="Button">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource PurpleColor}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource PurpleColor}"/>
        <Setter Property="Padding" Value="5,3"/>
        <!-- Выравнивание содержимого по центру -->
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="10"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <!-- ContentPresenter с выравниванием по центру -->
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource PinkHoverColor}"/>
                            <Setter Property="Foreground" Value="{StaticResource PurpleColor}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource PinkPressedColor}"/>
                            <Setter Property="Foreground" Value="{StaticResource PurpleColor}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource DisabledBackgroundColor}"/>
                            <Setter Property="Foreground" Value="{StaticResource DisabledForegroundColor}"/>
                            <Setter Property="BorderBrush" Value="#D3D3D3"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>